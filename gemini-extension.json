{
  "name": "table",
  "version": "0.0.3",
  "description": "JSON-driven SQLite operations for LLM workflows. Create tables, insert rows, join, group by, and run arbitrary SQL.",
  "contextFileName": "TABLE.md",
  "mcpServers": {
    "table": {
      "command": "node",
      "args": ["${extensionPath}${/}dist${/}server.js"],
      "cwd": "${extensionPath}"
    }
  }
}
