{
  "name": "table-extension",
  "version": "0.0.2",
  "type": "module",
  "description": "MCP extension for JSON-driven SQLite operations",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "files": [
    "server.js",
    "sqltool.py",
    "TABLE.md",
    "gemini-extension.json"
  ],
  "scripts": {
    "start": "node server.js",
    "build": "esbuild server.js --bundle --platform=node --format=esm --outfile=dist/server.js --external:node:* && cp sqltool.py dist/",
    "test": "node --test test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
